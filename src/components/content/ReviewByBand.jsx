/* TODO
- search bar (hw4)
- search button (once hit, shows average rating of the band if reviews exist)
- default displays 10 most recent band reviews
- After search display:
    - band name
    - average rating
    - paginate reviews on band (10 per page)
*/

export default function ReviewByBand (props) {
    return <div>
        <h1>Reviews by band</h1>
    </div>
}